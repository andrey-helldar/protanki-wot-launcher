<Window x:Name="MainProject" x:Class="_Hell_WPF_Multipack_Launcher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Multipack Launcher" Height="600" Width="800" ResizeMode="NoResize" WindowStartupLocation="CenterScreen" Icon="Resources/WOT.ico" WindowStyle="None" Loaded="Window_Loaded" Closing="Window_Closing">
    <Window.Effect>
        <DropShadowEffect BlurRadius="20" ShadowDepth="0"/>
    </Window.Effect>
    <Window.Background>
        <ImageBrush ImageSource="Resources/background.png"/>
    </Window.Background>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="262"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="34"/>
            <RowDefinition Height="165*"/>
            <RowDefinition Height="199*"/>
            <RowDefinition Height="123*"/>
            <RowDefinition Height="79"/>
        </Grid.RowDefinitions>
        <Frame x:Name="MainFrame" Margin="0" Grid.RowSpan="5" NavigationUIVisibility="Hidden"/>
        <Button x:Name="bPlay" Grid.Column="1" Grid.Row="4" Width="188" Style="{DynamicResource ButtonStylePlay}" FontSize="20" Click="bPlay_Click" FontFamily="Xolonium" HorizontalAlignment="Right" />
        <Button x:Name="bClose" Content="Close" Grid.Column="1" Style="{DynamicResource ButtonStyleClose}" HorizontalAlignment="Right" Margin="0,0,3,3" VerticalAlignment="Bottom" Width="30" Click="bClose_Click" Height="30"/>
        <Button x:Name="bMinimize" Content="Minimize" HorizontalAlignment="Right" Style="{DynamicResource ButtonStyleMin}" Margin="0,1,41,0" VerticalAlignment="Top" Width="30" Click="bMinimize_Click" Grid.Column="1" Height="30"/>
        <Button Grid.Column="1" HorizontalAlignment="Right" Style="{DynamicResource ButtonStyleVideoRight}" Margin="0,0,13,18" Grid.Row="1" VerticalAlignment="Bottom" Width="40"/>
        <Button Grid.Column="1" HorizontalAlignment="Left" Style="{DynamicResource ButtonStyleVideoLeft}" Margin="13,0,0,18" Grid.Row="1" VerticalAlignment="Bottom" Width="40"/>
        <Rectangle Grid.Column="1" Fill="Red" Opacity="0.5" HorizontalAlignment="Left" Height="36" Margin="18,5,0,0" Grid.Row="4" Width="36"/>
        <Button x:Name="bLauncherWOT" Grid.Column="1" Style="{DynamicResource ButtonStyleLogotype}" Margin="71,10,71,0" Grid.Row="1" VerticalAlignment="Top" Width="120" Height="120" Click="bLauncherWOT_Click"/>
        <Button x:Name="bAirus" Grid.Column="1" Style="{DynamicResource ButtonStyleAIRUS}" Margin="0,30,0,0" Grid.Row="3" VerticalAlignment="Top" Width="212" Height="68" Click="bAirus_Click"/>
        <Frame Grid.Column="1" Height="180" Margin="0" Grid.Row="2" Background="Red" Opacity="0.5" Width="250"/>
    </Grid>
</Window>
