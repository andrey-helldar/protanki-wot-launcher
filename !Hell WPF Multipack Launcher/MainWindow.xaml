<Window x:Class="_Hell_WPF_Multipack_Launcher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Multipack Launcher" Height="600" Width="800" ResizeMode="NoResize" WindowStartupLocation="CenterScreen" Icon="Resources/WOT.ico" WindowStyle="None" Loaded="Window_Loaded">
    <Window.Effect>
        <DropShadowEffect BlurRadius="20" ShadowDepth="0"/>
    </Window.Effect>
    <Window.Background>
        <ImageBrush ImageSource="Resources/background.png"/>
    </Window.Background>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="262"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="34"/>
            <RowDefinition/>
            <RowDefinition Height="79"/>
        </Grid.RowDefinitions>
        <Frame x:Name="MainFrame" Margin="0" Grid.RowSpan="3"/>
        <Button x:Name="bPlay" Grid.Column="1" Grid.Row="2" Width="188" Style="{DynamicResource ButtonStylePlay}" FontSize="20" Click="bPlay_Click" FontFamily="Xolonium" HorizontalAlignment="Right" />
        <Button x:Name="bClose" Content="Close" Grid.Column="1" Style="{DynamicResource ButtonStyleCloseMin}" HorizontalAlignment="Right" Margin="0,0,3,3" VerticalAlignment="Bottom" Width="30" Click="bClose_Click" Height="30"/>
        <Button x:Name="bMinimize" Content="Minimize" HorizontalAlignment="Right" Style="{DynamicResource ButtonStyleCloseMin}" Margin="0,1,41,0" VerticalAlignment="Top" Width="30" Click="bMinimize_Click" Grid.Column="1" Height="30"/>

    </Grid>
</Window>
